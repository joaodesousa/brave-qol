# OS cruft
Thumbs.db
desktop.ini
.DS_Store
store-assets/.render/

# Packaged extension builds and signing keys
dist/
# Never commit a .pem. It is the private key that identifies the extension.
*.zip
*.crx
*.pem
